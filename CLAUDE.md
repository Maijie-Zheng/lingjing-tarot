# 灵境开发指南

> 🤖 本文件给 AI 开发工具（Claude Code、Cursor、Copilot）阅读。写任何代码前先读这个。

---

## 产品定位

灵境是一个 AI 塔罗解读 Web App。核心流程：

```
首页 → 输入问题 → 洗牌选牌（3张）→ 翻牌动画 → AI 解读 → 分享卡片
```

**核心验证问题：用户觉得 AI 解读"懂自己"吗？**

---

## 技术约束

### 必须遵守

- **React 18** + **Vite** + **JavaScript（JSX）**——不要用 TypeScript
- **Tailwind CSS** 做样式——不要引入其他 CSS 框架
- **Framer Motion** 做动画——翻牌、页面过渡用这个
- **React Router v6** 做路由——5 个页面需要
- **localStorage** 做存储——不做后端、不做数据库
- **DeepSeek API** 做 AI 解读——API Key 从 `.env` 读取
- **移动端优先**——所有页面以 375px 宽度为基准设计

### 坚决不做

- ❌ 不做登录/注册
- ❌ 不做付费/会员
- ❌ 不做社区/评论
- ❌ 不做后端服务器
- ❌ 不做数据库
- ❌ 不做微信小程序
- ❌ 不做多语言（只做简体中文）
- ❌ 不做复杂牌阵（MVP 只有三牌阵）

### MVP 范围

| 做 | 不做 |
|----|------|
| 6 个页面（首页/输入/抽牌/解读/历史/分享卡片） | 星座、八字、其他玄学 |
| 三牌牌阵（过去-现在-未来） | 单牌/凯尔特十字等其他牌阵 |
| 22 张大阿尔卡纳 | 56 张小阿尔卡纳 |
| 移动端 Web | 桌面端优化 |

---

## 关键文件

| 文件 | 用途 | 什么时候读 |
|------|------|----------|
| `docs/06-PRD产品需求文档.md` | 每个页面的详细规格 | 写页面之前 |
| `docs/08-AI-Prompt设计文档.md` | System Prompt + 测试用例 | 集成 AI 之前 |
| `docs/09-Demo开发记录.md` | 技术架构 + 组件树 + 开发计划 | 写代码之前 |
| `.env` | DeepSeek API Key | 不要硬编码 Key |

---

## 设计规范（快速参考）

| 属性 | 值 |
|------|-----|
| 背景色 | `#1a0a2e` → `#0d0221` 深紫渐变 |
| 品牌色（金色） | `#c9a96e` |
| 辅助色（紫色） | `#8b5cf6` |
| 标题字体 | Noto Serif SC（衬线体） |
| 正文字体 | PingFang SC / Noto Sans SC |
| 正文大小 | 16px，行高 1.8 |
| 按钮圆角 | 12px |
| 卡片圆角 | 12px |
| 页面水平 padding | 20px |

---

## 代码规范

- 每个组件一个文件，组件名用 PascalCase
- 页面组件放 `src/pages/`，通用组件放 `src/components/`
- 静态数据（牌面信息）放 `src/data/`
- API 调用、存储、图片生成等工具函数放 `src/utils/`
- 自定义 Hook 放 `src/hooks/`
- 所有文案用简体中文
- 注释用中文
- 引入新依赖前先确认是否必要——保持 `package.json` 轻量

---

## 开发顺序

按 7 轮迭代：

| 轮次 | 内容 | 状态 |
|------|------|------|
| 1 | Vite 项目 + 路由 + 5 个空页面 | ⬜ 待开始 |
| 2 | 首页/输入页/历史页静态版 | ⬜ |
| 3 | 洗牌 + 选牌 + 翻牌动画 | ⬜ |
| 4 | DeepSeek API + 解读逐段显示 | ⬜ |
| 5 | localStorage + 历史记录 | ⬜ |
| 6 | Canvas 分享卡片生成 | ⬜ |
| 7 | 动画打磨 + Vercel 部署 | ⬜ |

---

## 当前状态

**第 1 轮开发中：搭建项目骨架。**
